spring:
  threads:
    virtual:
      enabled: true
  application:
    name: cs-api

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

security:
  internal:
    token: ${INTERNAL_API_TOKEN}

# Nginx Basic Auth 계정은 DB(admin_member)를 기준으로 관리합니다.
admin:
  htpasswd-path: ${ADMIN_HTPASSWD_PATH:/app/config/.htpasswd}

naver:
  session:
    id: ${NAVER_CAFE_SESSION_ID:9f2b4d68-4d2c-4db6-a9ec-285809470036}
    renew-trigger-url: ${NAVER_SESSION_RENEW_TRIGGER_URL:}

s3:
  endpoint: ${S3_ENDPOINT:http://localhost:9000}
  access-key: ${S3_ACCESS_KEY}
  secret-key: ${S3_SECRET_KEY}
  bucket: ${S3_BUCKET:kakao-webhook-logs}
  region: ${S3_REGION:ap-northeast-2}
  external-url: ${S3_EXTERNAL_URL:http://localhost:9000}

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,loggers
      base-path: /internal/actuator
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
  metrics:
    tags:
      application: ${spring.application.name}
